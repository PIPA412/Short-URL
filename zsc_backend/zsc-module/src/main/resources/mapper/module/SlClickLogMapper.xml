<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zsc.module.mapper.SlClickLogMapper">

    <insert id="incrementDailyClick">
        INSERT INTO sl_click_log (link_id, click_date, click_count)
        VALUES (#{linkId}, #{clickDate}, 1)
        ON DUPLICATE KEY UPDATE click_count = click_count + 1
    </insert>

</mapper>
